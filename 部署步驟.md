# 部署步驟

1. 解壓縮 zip。
2. 將內容放到 GitHub 專案第一層。
3. 執行：

```bash
git add .
git commit -m "fix ARC V13 stats tables search input and admin settings"
git push origin main
```

4. Vercel 會自動重新部署。

本次只修改前端顯示、搜尋輸入與 app_state JSON 設定結構，不需要重新執行 Supabase SQL。
