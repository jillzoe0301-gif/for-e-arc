# ARC V13 正式版部署步驟

## 1. Supabase 建立資料表與後端函式

打開 Supabase → SQL Editor，執行：

```text
supabase/001_formal_schema_rpc.sql
```

這個 SQL 會建立：

```text
arc_users
arc_sessions
arc_app_state
arc_login
arc_get_state
arc_save_state
arc_list_users
arc_admin_create_user
arc_admin_update_user
arc_admin_set_user_active
arc_admin_delete_user
arc_logout
```

## 2. Vercel 環境變數

到 Vercel 專案 → Settings → Environment Variables，新增：

```text
VITE_SUPABASE_URL=https://你的-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=你的 Supabase anon public key
```

不要放 service_role key。

## 3. GitHub 第一層檔案

不要直接把 ZIP 放進 GitHub。請先解壓縮，然後把資料夾內檔案放到 GitHub 專案第一層。

第一層應該看到：

```text
index.html
package.json
package-lock.json
README.md
部署步驟.md
.env.example
supabase/001_formal_schema_rpc.sql
```

## 4. 推上 GitHub

```bash
git add .
git commit -m "add ARC V13 amount 500 step select and paste"
git push origin main
```

## 5. Vercel 設定

```text
Framework Preset：Vite
Root Directory：./
Build Command：npm run build
Output Directory：dist
Install Command：npm install --no-audit --no-fund --legacy-peer-deps
Development Command：npm run dev
```

## 6. 預設登入帳號

密碼統一：`123456`

```text
若儀 管理員 jillzoe@forwardhrm.com.tw
嘉陽 管理員 patty@forwardhrm.com.tw
明書 管理員 mint@forwardhrm.com.tw
詩涵 行政 rachel@forwardhrm.com.tw
佩珊 行政 penny@forwardhrm.com.tw
晏婷 行政 helen@forwardhrm.com.tw
奕君 行政 jean_guo@forwardhrm.com.tw
莞莞 行政 maru@forwardhrm.com.tw
芸瑄 會計 nina@forwardhrm.com.tw
淑娥 會計 joy@forwardhrm.com.tw
```

## 7. 上線後確認

登入後請確認：

```text
1. 右上角顯示登入者與角色
2. 管理員看得到全部選單
3. 行政看不到會計對帳、會計查詢、仲介與扣款帳號、系統設定
4. 會計只看得到會計對帳與會計查詢
5. 選案繳費不同仲介不可同批
6. 批次送件可貼上承辦、仲介別、日期與金額整欄；日期會顯示 YYYY-MM-DD，金額會整理成 500 級距
7. 新增或修改資料後，換另一台電腦登入也看得到
```

## 8. 注意

本版為 V13 正式版，會連線 Supabase 共用資料。若沒有設定 Supabase 環境變數，正式版會阻擋登入。


## 已部署但登入失敗的修正

如果畫面顯示：

```text
function crypt(text, text) does not exist
```

請在 Supabase → SQL Editor 執行：

```text
supabase/002_fix_pgcrypto_login_and_hide_login_hint.sql
```

執行後不用重建資料表，只要重新整理登入頁再登入。
