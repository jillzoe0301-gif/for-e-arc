# 部署步驟

1. 解壓縮本檔。
2. 將所有檔案放到 GitHub 專案第一層。
3. 執行：

```bash
git add .
git commit -m "fix ARC V13 search response without breaking IME"
git push origin main
```

4. Vercel 會自動重新部署。

本版只修改前端搜尋與畫面顯示，不需要重新跑 Supabase SQL。
