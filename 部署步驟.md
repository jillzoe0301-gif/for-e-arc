# 部署步驟

1. 解壓縮本檔案。
2. 將資料夾內檔案放到 GitHub 專案第一層。
3. 執行：

```bash
git add .
git commit -m "fix ARC V13 data change batch paste and account balances"
git push origin main
```

4. Vercel 會自動重新部署。

本次不用重新跑 Supabase SQL。
