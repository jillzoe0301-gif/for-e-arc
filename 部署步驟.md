# 部署步驟

1. 解壓縮本檔案。
2. 將資料夾內檔案放到 GitHub 專案第一層。
3. 執行：

```bash
git add .
git commit -m "update ARC V13 accounting reconciliation and fax pickup controls"
git push origin main
```

4. Vercel 會自動部署。

## 注意

- 本次不用重新跑 Supabase SQL。
- 保留既有案件新增、案件查詢、列印、統計、日期格式、金額格式、承辦 / 仲介欄位與原本資料儲存邏輯。
