# 部署步驟

1. 解壓縮此 zip。
2. 將裡面的檔案放到 GitHub 專案第一層。
3. 執行：

```bash
git add .
git commit -m "fix ARC V13 left menu buttons after IME search fix"
git push origin main
```

4. 等待 Vercel 自動部署完成。

本次只修正前端選單與中文輸入 render 鎖定，不需要重新跑 Supabase SQL。
