# ARC V13 正式版｜搜尋輸入與中文注音修正版

本版修正全系統搜尋 / 查詢 / 查找 / 篩選欄位：

- 中文注音組字期間不執行搜尋、不重建輸入框。
- compositionend 後才將 inputText 寫入 searchKeyword 並執行搜尋。
- 搜尋時優先只更新搜尋結果區，不重建包含 input 的父層頁面。
- 修正搜尋變成輸入後沒有反應的問題。
- 保留左側選單修正，選單點擊仍可正常切換。

不用重新執行 Supabase SQL。
