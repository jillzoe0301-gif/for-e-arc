# 部署步驟

1. 解壓縮本檔案。
2. 將內容放到 GitHub 專案第一層。
3. 執行：

```bash
git add .
git commit -m "fix ARC V13 stats widths search input and receipt order duplicate check"
git push origin main
```

4. Vercel 會自動重新部署。

本次只修改前端顯示、搜尋輸入與收據順序檢查，不需要重新跑 Supabase SQL。
